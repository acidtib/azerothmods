<div class="row mt-4">
  <div class="col-12">
    <h1>Search</h1>
    <hr class="featurette-divider">
  </div>

  <div class="col-md-8 mb-4">
    <% @mods.each do |mod| %>
      <%= render partial: "cards", locals: { mod: mod } %>
    <% end %>

    <%= paginate @mods %>
  </div>
</div>